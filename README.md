# Custom RBAC Authentication & Authorization System

## 📌 О проекте
Данный проект представляет собой кастомную систему управления доступом на основе ролей (**RBAC**), реализованную на **Django** и **Django Rest Framework**. 

Главная особенность — полный отказ от стандартных механизмов авторизации Django (contrib.auth) в пользу собственного решения. Это позволяет гибко настраивать права доступа к любым бизнес-объектам через базу данных.

## 🏗 Архитектура системы доступа
Доступ в системе определяется связкой четырех сущностей:

1.  **User**: Пользователь системы. Поддерживает **Soft Delete** (мягкое удаление) через флаг `is_active`.
2.  **Role**: Роль пользователя (например, `Admin`, `User`, `Manager`).
3.  **BusinessObject**: Ресурс системы, доступ к которому нужно контролировать (например, `PRODUCT`, `ACCESS_RULE`).
4.  **AccessRule**: Центр управления правами. Содержит булевы флаги разрешений для каждой роли на конкретный объект:
    *   `can_create` — создание.
    *   `can_read_all` / `can_read_own` — просмотр всех или только своих записей.
    *   `can_update_all` / `can_update_own` — редактирование.
    *   `can_delete_all` / `can_delete_own` — удаление.

## 🔐 Безопасность и Аутентификация
*   **Пароли**: Хешируются с использованием алгоритма **bcrypt**.
*   **Аутентификация**: Используются **JWT-токены** (JSON Web Token). 
*   **Интеграция**: Вместо стандартного Middleware реализован кастомный класс `CustomJWTAuthentication` в DRF, который валидирует токен из заголовка `Authorization: Bearer <token>` и идентифицирует пользователя.

## 🛠 Технологический стек
*   **Python 3.x**
*   **Django 5.x / 6.x**
*   **Django Rest Framework**
*   **PyJWT** (работа с токенами)
*   **bcrypt** (хеширование паролей)
*   **SQLite** (база данных)

## 🚀 Быстрый запуск

## 1. Подготовка окружения
Склонируйте репозиторий и установите необходимые зависимости:
pip install django djangorestframework pyjwt bcrypt requests

## 2. Настройка базы данных
Выполните миграции и запустите скрипт инициализации, который создаст базовые роли, объекты и учетную запись администратора:
python manage.py migrate
python init_db.py
Данные для входа админа: admin@test.com / admin123

## 3. Запуск сервера
python manage.py runserver

## 4. Тестирование
Для автоматической проверки всех функций (регистрация, логин, проверка прав, мягкое удаление) запустите тестовый скрипт:
python test_api.py

## 📋 API Эндпоинты
| Метод | Эндпоинт | Описание | Доступ |
| :--- | :--- | :--- | :--- |
| **POST** | `/api/register/` | Регистрация нового пользователя | Всем |
| **POST** | `/api/login/` | Вход и получение JWT-токена | Всем |
| **GET** | `/api/profile/` | Получение данных своего профиля | Авторизованные |
| **PATCH** | `/api/profile/` | Редактирование своего профиля | Авторизованные |
| **DELETE** | `/api/profile/` | Мягкое удаление аккаунта | Авторизованные |
| **GET** | `/api/admin/rules/` | Список правил доступа | Только Админ |
| **GET** | `/api/products/` | Пример бизнес-объекта | По правилам RBAC |

## ⚙️ Логика проверки прав (Services)
Вся бизнес-логика авторизации вынесена в core/services.py. 
Функция has_permission собирает все роли пользователя и проверяет, разрешено ли запрашиваемое действие для данного объекта. 
Если пользователь не авторизован — возвращается 401, если прав недостаточно — 403 Forbidden.